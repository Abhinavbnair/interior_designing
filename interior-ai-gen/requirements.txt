# Phases 1-9
# Diffusion / vision (GPU runtime only — Kaggle/Colab)
torch>=2.2
diffusers>=0.27
transformers>=4.38
accelerate>=0.27
safetensors>=0.4

# Core (runs anywhere, including the local laptop)
pillow>=10.0
numpy>=1.24
pydantic>=2.0

# LLM/VLM provider (only needed for Phases 5-9; not required for Phases 1-3)
anthropic>=0.39

# Dev
pytest>=8.0

# Phase 10 (not yet implemented): fastapi, uvicorn, streamlit, opencv-python
